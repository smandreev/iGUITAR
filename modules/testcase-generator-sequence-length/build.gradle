sourceSets {
    main {
        java {
            srcDir 'src'
        }
        resources {
            srcDir 'resources'
        }
    }
}

dependencies {
    compile project(':gui-model-core')
    compile project(':testcase-generator-core')
    testCompile group: 'junit', name: 'junit', version: '4.11'
}